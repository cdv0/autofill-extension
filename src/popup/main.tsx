import ReactDOM from "react-dom/client";
import Popup from "./Popup";

ReactDOM.createRoot(document.getElementById("root")!).render(<Popup />); // Mounts the react app (Popup) into the HTML page. Fills the root div in main.html